{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-target-behind-origin.sh",
            "timeout": 10,
            "statusMessage": "Checking branch vs origin (harness stale-base guard)…"
          }
        ]
      }
    ]
  }
}
